menu "Rockchip GPIO drivers"

config GPIO_ROCKCHIP
	bool "GPIO for Rockchip RK3588"
	depends on GPIO
	depends on DT_HAS_ROCKCHIP_RK3588_GPIO_ENABLED
	default y
	help
	  Driver GPIO per i bank Rockchip stile RK3588 (32 pin/bank).

endmenu